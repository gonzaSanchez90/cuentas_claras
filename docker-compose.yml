services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - accounts_data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: always

volumes:
  accounts_data:
